<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Link</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <body class="font-sans bg-grey5">
    <div class="flex min-h-screen">
      <div class="flex-wrap w-0 lg:w-sidebar"></div>
      <div class="flex-grow w-full">
        <div>
          <header>
              <div class="text-center text-warning">
                <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
                <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
             </div>
            <nav class="flex items-center h-topbar sm:h-topbar-sm lg:h-topbar-lg">
              <div class="flex-grow pl-4 lg:pl-0">
                  <a href="/">
                    <img class="h-logo sm:h-logo-sm" src="<%= Routes.static_path(@conn, "/images/eyra-logo.svg") %>" alt="Eyra Link Logo"/>
                  </a>
              </div>
              <%= if !!!assigns[:hide_account_menu] do %>
                <%= if Pow.Plug.current_user(@conn) do %>
                    <div class="flex-none p-4 text-button font-bold">
                      <%= link dgettext("eyra-account", "profile.button"), class: "button-clear", to: Routes.user_profile_path(@conn, :edit) %>
                    </div>
                    <div class="flex-none pr-4">
                      <%= link dgettext("eyra-account", "signout.button"), class: "p-6 text-button font-bold", to: Routes.pow_session_path(@conn, :delete), method: :delete %>
                    </div>
                <% else %>
                    <div class="flex-none">
                      <%= link dgettext("eyra-account", "signin.button"), class: "p-6 text-button font-bold", to: Routes.pow_session_path(@conn, :new) %>
                    </div>
                <% end %>
              <% end %>
              <div class="flex-wrap pr-10 font-button font-bold">
                <%= for {locale, name} <- supported_languages() do %>
                  <%= link name, to: Routes.language_switch_path(@conn, :index, locale, redir: @conn.request_path) %>
                <% end %>
              </div>
            </nav>
          </header>
        </div>
        <div class="flex h-full bg-white">
          <div class="flex-wrap"></div>
          <div class="flex-grow h-full">
            <main role="main" class="h-full w-full">
              <%= @inner_content %>
            </main>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
