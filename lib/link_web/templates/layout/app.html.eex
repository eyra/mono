<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Link</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <body class="font-sans bg-grey5">
    <div class="flex min-h-screen">
<<<<<<< HEAD
      <div class="flex-wrap w-0 lg:w-sidebar"></div>
      <div class="flex-grow w-full">
        <div>
          <header>
              <div class="text-center text-warning">
                <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
                <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
             </div>
            <nav class="flex items-center h-topbar lg:h-topbar-lg">
              <div class="flex-grow pl-4 lg:pl-0">
                  <a href="/">
                    <img src="<%= Routes.static_path(@conn, "/images/eyra-logo.svg") %>" alt="Eyra Link Logo"/>
                  </a>
              </div>
              <div class="flex-wrap font-labels font-normal">
                  <%= gettext("Switch to") %>&nbsp;
              </div>
              <div class="flex-wrap pr-4 font-button font-bold">
                <%= for {locale, name} <- supported_languages() do %>
                  <%= link name, to: Routes.language_switch_path(@conn, :index, locale, redir: @conn.request_path) %>
                <% end %>
              </div>
              <div class="flex-none p-4">
              <%= if Pow.Plug.current_user(@conn) do %>
                  <%= primary_button dgettext("eyra-account", "signout.button"), Routes.pow_session_path(@conn, :delete), :delete %>
              <% else %>
                  <%= primary_button dgettext("eyra-account", "signin.button"), Routes.pow_session_path(@conn, :new), :get %>
              <% end %>
              </div>
=======
      <div class="flex-wrap w-0 md:w-sidebar"></div>
      <div class="flex-grow w-full">
        <div>
          <header class="" >
            <div>
              <%= gettext("Switch to:") %>&nbsp;
              <%= for {locale, name} <- supported_languages() do %>
                <%= link name, to: Routes.language_switch_path(@conn, :index, locale, redir: @conn.request_path) %>
              <% end %>
            </div>
            <div class="text-center text-warning">
              <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
              <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
            </div>
            <nav class="flex items-center h-topbar">
              <div class="flex-grow pl-4 md:pl-0">
                <a href="/">
                  <img src="<%= Routes.static_path(@conn, "/images/eyra-logo.svg") %>" alt="Eyra Link Logo"/>
                </a>
              </div>
              <%= if Pow.Plug.current_user(@conn) do %>
                <div class="flex-none p-4">
                  <%= link "Edit Profile", class: "button-clear", to: Routes.user_profile_path(@conn, :edit) %>
                  <%= primary_button dgettext("eyra-account", "signout.button"), Routes.pow_session_path(@conn, :delete), :delete %>
                </div>
              <% end %>
>>>>>>> 4458f746c945a7336c3757d13cfc790c39cac5ba
            </nav>
          </header>
        </div>
        <div class="flex h-full bg-white">
          <div class="flex-wrap"></div>
          <div class="flex-grow h-full">
            <main role="main" class="h-full w-full">
              <%= @inner_content %>
            </main>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
