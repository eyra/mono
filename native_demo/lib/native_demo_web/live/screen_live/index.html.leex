<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <%= csrf_meta_tag() %>
    <%= live_title_tag assigns[:page_title] || "NativeDemo", suffix: " · Phoenix Framework" %>
    <script defer phx-track-static type="text/javascript" src="<%= Routes.static_path(@socket, "/js/app.js") %>"></script>
    <style type="text/css">
      body,a { color: white; }
      .main { width: 100%; height: 100%; padding: 4em; }
    </style>
  </head>
  <body>
    <div style="background-color: #<%= @color %>" class="main native-settings" 
         data-native-title="Nav depth <%= @depth %>, modal depth: <%= @modal_depth %>">
      <div>Nav depth <%= @depth %>, modal depth: <%= @modal_depth %></div>
      <h2 id="title"></h2>
      <p>Basic screen navigation demo.</p>
      <div>
        <div>
          <%= live_redirect "Open regular screen", to: Routes.screen_index_path(@socket, :index, nav_stack: @push_nav_stack) %>
        </div>
        <div>
          <%= live_redirect "Close regular screen", to: Routes.screen_index_path(@socket, :index, nav_stack: @pop_nav_stack) %>
        </div>
        <hr>
        <div>
          <%= live_redirect "Open modal screen", to: Routes.screen_index_path(@socket, :index, nav_stack: @push_modal_nav_stack), data: [native_operation: :push_modal] %>
        </div>
        <div>
          <%= live_redirect "Close modal screen", to: Routes.screen_index_path(@socket, :index, nav_stack: @pop_modal_nav_stack), data: [native_operation: :pop_modal] %>
        </div>
      </div>
    </div>
  </body>
</html>

